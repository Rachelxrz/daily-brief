name: Daily Brief + Market Monitor

on:
  schedule:
    - cron: '0 13 * * *'       # 08:00 ET - News brief (daily)
    - cron: '30 14 * * 1-5'    # 09:30 ET - Market monitor (weekdays)
    - cron: '0 23 * * *'       # 18:00 ET - Evening news brief (daily)
    - cron: '30 23 * * 1-5'    # 18:30 ET - Evening market monitor (weekdays)
    - cron: '30 20 * * 1-5'    # 16:30 ET - Congress signal (weekdays, post-market)
    - cron: '0 13 * * 1-5'     # 08:00 ET - Wheel Strategy scan (weekdays, pre-market)
    - cron: '0 12 * * 1-5'     # 08:00 ET - Signal Advisor pre-market (weekdays)
    - cron: '30 21 * * 1-5'    # 17:30 ET - Signal Advisor post-market (weekdays)
    - cron: '30 21 * * 5'      # 每周五 17:30 ET（收盘后）- Prediction 轮动观察（每周）
  workflow_dispatch:
    inputs:
      job_type:
        description: 'Job to run'
        required: true
        default: 'both'
        type: choice
        options: [both, news_only, market_only, congress_only, wheel_only, signal_pre, signal_post, prediction]
      dry_run:
        description: 'Dry run (no push)'
        required: false
        default: 'false'
        type: boolean

env:
  WECOM_WEBHOOK_URL:      ${{ secrets.WECOM_WEBHOOK_URL }}
  WXPUSHER_APP_TOKEN:     ${{ secrets.WXPUSHER_APP_TOKEN }}
  WXPUSHER_UIDS:          ${{ secrets.WXPUSHER_UIDS }}
  SERVERCHAN_SENDKEY:     ${{ secrets.SERVERCHAN_SENDKEY }}
  ANTHROPIC_API_KEY:      ${{ secrets.ANTHROPIC_API_KEY }}

# 所有 run 串行化：同一时间只跑一个，其余排队，避免多个 job 并发 push 同一分支
# 时相互挤掉（[remote rejected] cannot lock ref）。cancel-in-progress:false = 排队等待，
# 不取消正在跑的。
concurrency:
  group: daily-brief-main-push
  cancel-in-progress: false

jobs:

  daily-news-brief:
    name: Daily News Brief
    runs-on: ubuntu-latest
    timeout-minutes: 35
    if: >-
      (github.event_name == 'schedule' && (github.event.schedule == '0 13 * * *' || github.event.schedule == '0 23 * * *')) ||
      (github.event_name == 'workflow_dispatch' && (github.event.inputs.job_type == 'both' || github.event.inputs.job_type == 'news_only'))

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Create directories
        run: mkdir -p logs docs

      - name: Run daily news brief
        run: |
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            python main.py --dry-run
          else
            python main.py
          fi

      - name: Run analyst watch
        if: github.event.inputs.dry_run != 'true'
        run: python analyst_watch.py
        continue-on-error: true

      - name: Archive brief to briefs/YYYY/MM/（C1:每日简报长存,只追加;失败不拖垮管线）
        if: github.event.inputs.dry_run != 'true'
        run: python scripts/archive_brief.py
        continue-on-error: true

      - name: Commit & push web data
        if: github.event.inputs.dry_run != 'true'
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          cp docs/data.json /tmp/new_data.json
          # 分析师历史（三个月留存）必须一起提交，否则下次 checkout 会丢失
          [ -f analyst_history.jsonl ] && cp analyst_history.jsonl /tmp/analyst_history.jsonl || true
          # briefs 归档暂存(reset --hard 会清掉工作区,先存后回)
          [ -d briefs ] && cp -r briefs /tmp/briefs_new || true
          git fetch origin main
          git reset --hard origin/main
          python merge_data.py /tmp/new_data.json docs/data.json
          [ -f /tmp/analyst_history.jsonl ] && cp /tmp/analyst_history.jsonl analyst_history.jsonl || true
          [ -d /tmp/briefs_new ] && cp -rn /tmp/briefs_new/. briefs/ || true
          git add docs/data.json analyst_history.jsonl
          [ -d briefs ] && git add briefs || true
          git diff --staged --quiet || git commit -m "Daily news brief $(date +'%Y-%m-%d')"
          git push

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: news-logs-${{ github.run_number }}
          path: logs/
          retention-days: 7

  market-monitor:
    name: Market Structure Monitor
    runs-on: ubuntu-latest
    timeout-minutes: 25
    if: >-
      (github.event_name == 'schedule' && (github.event.schedule == '30 14 * * 1-5' || github.event.schedule == '30 23 * * 1-5')) ||
      (github.event_name == 'workflow_dispatch' && (github.event.inputs.job_type == 'both' || github.event.inputs.job_type == 'market_only'))

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Create directories
        run: mkdir -p logs docs

      - name: Run market structure monitor
        run: |
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            python market_monitor.py --dry-run
          else
            python market_monitor.py
          fi

      - name: Commit & push web data
        if: github.event.inputs.dry_run != 'true'
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          cp docs/data.json /tmp/new_data.json
          git fetch origin main
          git reset --hard origin/main
          python merge_data.py /tmp/new_data.json docs/data.json
          git add docs/data.json
          git diff --staged --quiet || git commit -m "Market monitor $(date +'%Y-%m-%d')"
          git push

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: market-logs-${{ github.run_number }}
          path: logs/
          retention-days: 7

  congress-signal:
    name: 🏛 国会交易信号
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: >-
      (github.event_name == 'schedule' && github.event.schedule == '30 20 * * 1-5') ||
      (github.event_name == 'workflow_dispatch' && (github.event.inputs.job_type == 'both' || github.event.inputs.job_type == 'congress_only'))

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Create directories
        run: mkdir -p logs docs docs/data

      - name: Run congress tracker
        run: |
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            python congress_tracker.py --dry-run
          else
            python congress_tracker.py
          fi

      - name: Commit & push web data
        if: github.event.inputs.dry_run != 'true'
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          cp docs/data.json /tmp/new_data.json
          [ -f docs/data/congress_seen.json ] && cp docs/data/congress_seen.json /tmp/congress_seen.json || true
          git fetch origin main
          git reset --hard origin/main
          python merge_data.py /tmp/new_data.json docs/data.json
          [ -f /tmp/congress_seen.json ] && cp /tmp/congress_seen.json docs/data/congress_seen.json || true
          git add docs/data.json docs/data/congress_seen.json
          git diff --staged --quiet || git commit -m "congress_tracker $(date +'%Y-%m-%d')"
          git push

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: congress-logs-${{ github.run_number }}
          path: logs/
          retention-days: 7

  update-status:
    name: ⚙️ Update PROJECT_STATUS
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [daily-news-brief, market-monitor]
    if: always() && github.event.inputs.dry_run != 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0          # 需要完整 git 历史来生成变更日志

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Refresh status
        run: python update_status.py

      - name: Commit & push status
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          cp PROJECT_STATUS.md /tmp/PROJECT_STATUS.md
          git fetch origin main
          git reset --hard origin/main
          cp /tmp/PROJECT_STATUS.md PROJECT_STATUS.md
          git add PROJECT_STATUS.md
          git diff --staged --quiet || git commit -m "Update PROJECT_STATUS $(date +'%Y-%m-%d')"
          git push

  wheel-strategy:
    name: 🎡 Wheel Strategy
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: >-
      (github.event_name == 'schedule' && github.event.schedule == '0 13 * * 1-5') ||
      (github.event_name == 'workflow_dispatch' && (github.event.inputs.job_type == 'both' || github.event.inputs.job_type == 'wheel_only'))

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Create directories
        run: mkdir -p logs docs docs/data

      - name: Run wheel strategy
        run: |
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            python wheel_strategy.py --dry-run
          else
            python wheel_strategy.py
          fi

      - name: Commit & push web data
        if: github.event.inputs.dry_run != 'true'
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          cp docs/data.json /tmp/new_data.json
          git fetch origin main
          git reset --hard origin/main
          python merge_data.py /tmp/new_data.json docs/data.json
          git add docs/data.json
          git diff --staged --quiet || git commit -m "wheel_strategy $(date +'%Y-%m-%d')"
          git push

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: wheel-logs-${{ github.run_number }}
          path: logs/
          retention-days: 7

  signal-advisor-premarket:
    name: 📊 技术信号·核心（盘前）
    runs-on: ubuntu-latest
    timeout-minutes: 12
    if: >-
      (github.event_name == 'schedule' && github.event.schedule == '0 12 * * 1-5') ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job_type == 'signal_pre')
    steps:
      - uses: actions/checkout@v4
        with: { token: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: actions/setup-python@v5
        with: { python-version: '3.11', cache: 'pip' }
      - run: pip install -r requirements.txt
      - run: mkdir -p logs docs
      - run: python signal_advisor.py --session pre --group priority
      - name: Commit & push
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          cp docs/data.json /tmp/new_data.json
          git fetch origin main && git reset --hard origin/main
          python merge_data.py /tmp/new_data.json docs/data.json
          git add docs/data.json
          git diff --staged --quiet || git commit -m "signal_advisor_pre $(date +'%Y-%m-%d')"
          git push
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: signal-pre-logs-${{ github.run_number }}
          path: logs/
          retention-days: 7

  signal-advisor-postmarket-priority:
    name: 📊 技术信号·核心（盘后）
    runs-on: ubuntu-latest
    timeout-minutes: 12
    if: >-
      (github.event_name == 'schedule' && github.event.schedule == '30 21 * * 1-5') ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job_type == 'signal_post')
    steps:
      - uses: actions/checkout@v4
        with: { token: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: actions/setup-python@v5
        with: { python-version: '3.11', cache: 'pip' }
      - run: pip install -r requirements.txt
      - run: mkdir -p logs docs
      - run: python signal_advisor.py --session post --group priority
      - name: Run watchlist 闸门（每日：跌破周线150MA→移入Secondary Watchlist）
        run: python watchlist_gate.py
        continue-on-error: true
      - name: Run MA×Supertrend 均线信号
        run: python ma_cross_signal.py
        continue-on-error: true
      - name: Run QQQ 六因子宏观闸门
        run: python macro_credit_gate.py
        continue-on-error: true
      - name: Run 脆弱性/拥挤度侧栏 + 崩盘性质诊断（读 macro_gate 票数，须在其后）
        run: python tail_fragility.py
        continue-on-error: true
      - name: Run 达利欧泡沫指标 + 货币收紧扳机
        run: python dalio_inspired_bubble.py
        continue-on-error: true
      - name: Run 市场广度/集中度信号（Phase 2）
        run: python internals_state.py
        continue-on-error: true
      - name: Run 板块强弱·资金流向面板
        run: python sector_board.py
        continue-on-error: true
      - name: Commit & push
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          cp docs/data.json /tmp/new_data.json
          # 均线信号历史 + ETF缓存 + watchlist(闸门暂停状态) + 变更日志 必须一起提交，否则 reset 会丢
          [ -f ma_signal_history.json ] && cp ma_signal_history.json /tmp/ma_signal_history.json || true
          [ -f etf_flags.json ]         && cp etf_flags.json         /tmp/etf_flags.json         || true
          [ -f docs/watchlist.json ]           && cp docs/watchlist.json           /tmp/watchlist.json           || true
          [ -f docs/watchlist_changelog.json ] && cp docs/watchlist_changelog.json /tmp/watchlist_changelog.json || true
          git fetch origin main && git reset --hard origin/main
          python merge_data.py /tmp/new_data.json docs/data.json
          [ -f /tmp/ma_signal_history.json ] && cp /tmp/ma_signal_history.json ma_signal_history.json || true
          [ -f /tmp/etf_flags.json ]         && cp /tmp/etf_flags.json         etf_flags.json         || true
          [ -f /tmp/watchlist.json ]           && cp /tmp/watchlist.json           docs/watchlist.json           || true
          [ -f /tmp/watchlist_changelog.json ] && cp /tmp/watchlist_changelog.json docs/watchlist_changelog.json || true
          git add docs/data.json ma_signal_history.json etf_flags.json docs/watchlist.json docs/watchlist_changelog.json
          git diff --staged --quiet || git commit -m "signal_advisor_post_priority $(date +'%Y-%m-%d')"
          git push
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: signal-post-priority-logs-${{ github.run_number }}
          path: logs/
          retention-days: 7

  signal-advisor-postmarket-extended:
    name: 📊 技术信号·扩展（盘后）
    runs-on: ubuntu-latest
    timeout-minutes: 12
    needs: signal-advisor-postmarket-priority
    if: >-
      (github.event_name == 'schedule' && github.event.schedule == '30 21 * * 1-5') ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job_type == 'signal_post')
    steps:
      - uses: actions/checkout@v4
        with: { token: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: actions/setup-python@v5
        with: { python-version: '3.11', cache: 'pip' }
      - run: pip install -r requirements.txt
      - run: mkdir -p logs docs
      - run: python signal_advisor.py --session post --group extended
      - name: Commit & push
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          cp docs/data.json /tmp/new_data.json
          git fetch origin main && git reset --hard origin/main
          python merge_data.py /tmp/new_data.json docs/data.json
          git add docs/data.json
          git diff --staged --quiet || git commit -m "signal_advisor_post_extended $(date +'%Y-%m-%d')"
          git push
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: signal-post-extended-logs-${{ github.run_number }}
          path: logs/
          retention-days: 7

  prediction-watch:
    name: 📈 Prediction 轮动观察（每周）
    runs-on: ubuntu-latest
    timeout-minutes: 12
    if: >-
      (github.event_name == 'schedule' && github.event.schedule == '30 21 * * 5') ||
      (github.event_name == 'workflow_dispatch' && (github.event.inputs.job_type == 'prediction' || github.event.inputs.job_type == 'both'))
    steps:
      - uses: actions/checkout@v4
        with: { token: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: actions/setup-python@v5
        with: { python-version: '3.11', cache: 'pip' }
      - run: pip install -r requirements.txt
      - run: mkdir -p docs
      - run: python prediction_watch.py
      - name: Commit & push
        if: github.event.inputs.dry_run != 'true'
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          cp docs/data.json /tmp/new_data.json
          cp prediction_snapshots.jsonl /tmp/snap.jsonl
          git fetch origin main && git reset --hard origin/main
          python merge_data.py /tmp/new_data.json docs/data.json
          cp /tmp/snap.jsonl prediction_snapshots.jsonl
          git add docs/data.json prediction_snapshots.jsonl
          git diff --staged --quiet || git commit -m "prediction_watch $(date +'%Y-%m-%d')"
          git push
